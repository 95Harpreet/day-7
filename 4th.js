var emp=[12,34,322,11]

var i
var n=0
console.log('elements are:--------')
for (var chd of emp)     //for each loop(direct iteration of elements without need of array index)
{
    console.log(chd)
    n++
}
console.log('total elemenys are '+n)